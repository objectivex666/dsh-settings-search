# 更新日志

变更记录为双语：英文见 [CHANGELOG.en.md](./CHANGELOG.en.md)。

## v1.8.0

### ✨ 新功能
- **新用户引导界面**：首次打开设置面板时弹出引导弹窗，介绍实时搜索、拼音与意图联想、AI 联想、点击即达、自动导入索引与自带设置页等核心功能；可一键打开「设置搜索」页，并支持「不再显示」。仅在首次自动弹出，之后可在「设置搜索」页通过「新用户引导」按钮重新打开。
- **清空日志二次确认**：「清空日志」按钮需再次确认「确认清空」才会执行，避免误清空日志。
- **清除 API 配置**：AI 配置区新增「清除配置」按钮，可一键清除 Base URL、模型名与 API Key（需二次确认），便于快速重置。同时为 API Key 输入框禁用浏览器密码保存提示。
- **测试 API 服务**：AI 配置区新增「测试接口」按钮，发送最小请求验证 Base URL、模型名与 API Key 是否可用，并就地显示成功或失败原因；失败时按 HTTP 状态码/错误类型给出可读提示（如认证失败、无权限、接口不存在、限流、服务端错误、超时等）。
- **页内选项可搜索**：「创意工坊」等分区内的选项（如「启用创意工坊卡片」）现可直接搜索并点击跳转，命中后在对应页面高亮该选项。
- **启动自动索引**：首次打开设置面板时自动遍历各设置分区，静默读取分区内选项（如「启用创意工坊卡片」）并导入搜索索引，无需逐页手动打开即可直接搜索所有分区选项；读取完成后自动恢复当前所在分区，并在搜索框下方显示导入提示，完成后自动消失。

### 🐛 修复
- **搜索结果名称截断**：搜索结果中的设置项名称过长时不再以单行省略号截断，改为换行完整展示；并优化页内选项的标签提取，优先显示选项本名，去除多余的行内描述（如「关闭后停用试穿、应用与背景控件…」）。
- **AI 接口安全加固**：对 AI 服务 Base URL 进行校验，仅允许 http(s) 地址；非本机地址必须使用 https（避免明文泄露 API Key），并拒绝内嵌账号密码或危险协议（如 `javascript:`、`data:`）的地址。不安全地址会在「测试接口」中给出明确提示，且 AI 联想会跳过该请求。

## v1.7.2

### ✨ 新功能
- **Issue 提交指南**：README「反馈与贡献」章节新增提交 Issue 时的说明，提醒附带问题描述、复现步骤、日志（「设置搜索」页可导出）、环境信息与截图/录屏，便于定位与复现。

### 🐛 修复
- 纯文档完善，无功能修复。

## v1.7.1

### ✨ 新功能
- **查看更新说明**：在「检查更新」旁新增按钮，点击拉取 GitHub Release 更新内容并就地展示；拉取失败时自动跳转至 Release 页面。
- **欢迎提交 Issue**：README 新增「反馈与贡献」章节，邀请反馈 Bug、功能建议或疑问，也欢迎发起 Pull Request。

### 🐛 修复
- 「检查更新」按钮配色与「保存配置」统一，设置页视觉更一致。
- 完成 Codex Security 安全扫描：当前工作区 diff 未发现可报告漏洞，无安全回归。

---

## v1.7.0

### ✨ 新功能
- **AI 联想搜索**：没有本地匹配时，用你自配的模型（OpenAI 兼容 / Anthropic）理解搜索意图并推荐设置项；默认关闭，可在「设置搜索」页开启并配置。
- **日志与导出**：记录插件运行、搜索与 AI 请求的关键动作，可在「设置搜索」页导出 `.log`、复制或清空；不记录 API Key。
- **设置页重构**：仿 Vision Router 卡片样式，标题去掉版本号，支持收起成一列。
- **更新模式仿 Vision Router**：检查更新后展示精确版本的一键更新命令与 npx 变体，并提供「查看 release notes」入口。

### 🐛 修复
- 修复更新命令缺少 `--profile` 与精确版本号，避免更新到错误版本。
- 修复设置页标题多余版本号与布局对齐问题，面板现已可折叠。

---

_后续版本请保持中英文双语（同步更新 CHANGELOG.en.md）。_
